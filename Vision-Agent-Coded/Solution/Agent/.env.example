# UiPath project (token vars are written by `uipath auth`)
UIPATH_PROJECT_ID=<your-project-guid>

# OpenAI — used by the vision agent's ChatOpenAI (gpt-5-mini).
# In the cloud this is read from a UiPath Credential Asset (OpenAI-Key) instead.
OPENAI_API_KEY=sk-your-key-here
